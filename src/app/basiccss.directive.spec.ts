import { BasiccssDirective } from './basiccss.directive';

describe('BasiccssDirective', () => {
  it('should create an instance', () => {
    const directive = new BasiccssDirective();
    expect(directive).toBeTruthy();
  });
});
